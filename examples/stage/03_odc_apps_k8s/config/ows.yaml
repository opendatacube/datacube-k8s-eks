wms:
  annotations:
    iam.amazonaws.com/role: ${role_name}
database:
  database: ${db_name}
  host: ${db_hostname}
  existingSecret: ${db_secret}
awsCredentialsSecret: ${aws_creds_secret}
ingress:
  hosts:
    - ows.${domain_name}
    - ows.services.${domain_name}
    - nc-ows.${domain_name}
    - cached-alb.${domain_name}