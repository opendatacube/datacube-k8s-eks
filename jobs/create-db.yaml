database:
  adminSecret: psql-admin-secret
  database: datacube
  host: localhost
  port: 5432
  create: true
  # Optional
  # username: datacube
  # Optional
  # password: password
wms:
  enabled: false